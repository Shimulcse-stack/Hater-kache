# 🚀 VS Code-এ "হাতের কাছে" (HaterKache) প্রোজেক্ট রান করার সম্পূর্ণ নির্দেশিকা (Command Guide)

আপনার নিজের পিসি/কম্পিউটারে **VS Code** ব্যবহার করে এই প্রোজেক্টটি লোকালি রান করার জন্য নিচের ধাপগুলো অনুসরণ করুন।

---

## 📋 প্রয়োজনীয় বিষয়সমূহ (Prerequisites)

১. **Node.js** ইন্সটল থাকা লাগবে (Node v18+ বা v20+ বা v22+ সুপারিশকৃত)।
   - ইন্সটল করা না থাকলে [nodejs.org](https://nodejs.org/) থেকে LTS ভার্সন ডাউনলোড ও ইন্সটল করুন।
   - চেক করতে টার্মিনালে লিখুন: `node -v` এবং `npm -v`
২. **VS Code** (Visual Studio Code) ইন্সটল করা থাকতে হবে।

---

## 🛠️ স্টেপ-বাই-স্টেপ কমান্ড নির্দেশিকা (Step-by-Step Commands)

### Step 1: VS Code-এ প্রজেক্ট ফোল্ডার ওপেন করুন
1. VS Code খুলুন।
2. `File` > `Open Folder...` থেকে প্রজেক্ট ফোল্ডারটি সিলেক্ট করুন।
3. VS Code-এর ইনবিল্ট টার্মিনাল ওপেন করতে চাপুন: **`Ctrl + ~`** (অথবা উপরের মেনু থেকে `Terminal` > `New Terminal`)

---

### Step 2: সকল ডিপেনডেন্সি ইন্সটল করুন (npm install)
টার্মিনালে নিচের কমান্ডটি লিখে Enter চাপুন:

```bash
npm install
```
*(এটি `package.json`-এ থাকা React, Vite, Express, Tailwind CSS ইত্যাদি লাইব্রেরি ডাউনলোড করে নেবে।)*

---

### Step 3: এনভায়রনমেন্ট ভেরিয়েবল সেটআপ (.env File Setup)
১. প্রজেক্টের রুট ফোল্ডারে একটি নতুন ফাইল তৈরি করুন নাম দিন: `.env`
২. `.env` ফাইলে নিচের লাইনটি বসান (আপনার Gemini API Key থাকলে দিতে পারেন, না দিলে ডিফল্ট সিমুলেটেড এআই রেসপন্স কাজ করবে):

```env
GEMINI_API_KEY="your_actual_gemini_api_key_here"
```

*(আপনার কাছে Gemini API Key না থাকলে খালি রাখতে পারেন বা লাইনটি এড়িয়ে যেতে পারেন।)*

---

### Step 4: ডেভেলপমেন্ট সার্ভার চালু করুন (Run Development Server)
ডেভেলপমেন্ট মোডে প্রজেক্ট চালু করতে টার্মিনালে লিখুন:

```bash
npm run dev
```

চালু হলে টার্মিনালে দেখতে পাবেন:
```text
Server running on http://0.0.0.0:3000
```

---

### Step 5: ব্রাউজারে অ্যাপ দেখুন
আপনার ব্রাউজার (Chrome / Firefox / Edge) ওপেন করুন এবং এড্রেস বারে যান:

👉 **[http://localhost:3000](http://localhost:3000)**

---

## 📦 প্রডাকশন বিল্ড ও স্টার্ট করতে চাইলে (Production Build & Run)

লাইভ প্রডাকশন মোডে চালাতে চাইলে নিচের কমান্ডগুলো দিন:

```bash
# ১. প্রোজেক্ট বিল্ড করুন
npm run build

# ২. প্রডাকশন সার্ভার স্টার্ট করুন
npm start
```

---

## 🛠️ অন্যান্য দরকারী কমান্ডসমূহ (Useful Helper Commands)

| কমান্ড | কাজ (Purpose) |
|---|---|
| `npm run dev` | ডেভেলপমেন্ট মোডে দ্রুত সার্ভার চালু করা |
| `npm run lint` | টাইপস্ক্রিপ্ট টাইপ চেক করা (`tsc --noEmit`) |
| `npm run build` | প্রোডাকশনের জন্য কোড বান্ডেল/বিল্ড করা |
| `npm start` | বিল্ড ফাইল থেকে সরাসরি নোড সার্ভার চালানো |

---

## ⚠️ সম্ভাব্য সমস্যা ও সমাধান (Troubleshooting Tips)

১. **`Could not find a declaration file for module 'react'` বা লাল দাগ (TypeScript Red Lines):**
   - এটি হওয়ার কারণ আপনার কম্পিউটারে প্রজেক্টের ডিপেনডেন্সি (প্যাকেজ) এখনও সম্পূর্ণ ইন্সটল করা হয়নি।
   - VS Code টার্মিনালে সরাসরি এই কমান্ডটি চালান:
     ```bash
     npm install
     ```
   - ইন্সটল শেষ হলে VS Code টি একবার ক্লোজ করে আবার ওপেন করুন (অথবা `Ctrl + Shift + P` চেপে `TypeScript: Restart TS Server` এ চাপুন)। সব লাল দাগ এবং এরর চলে যাবে!

২. **Port 3000 is already in use (3000 পোর্ট অন্য কিছুতে ব্যবহার হচ্ছে):**
   - কম্পিউটার রিস্টার্ট দিন অথবা পোর্ট ৩০০০ মুক্ত করে `npm run dev` পুনরায় দিন।
৩. **Node/npm Error:**
   - টার্মিনালে `node -v` দিয়ে নিশ্চিত হন আপনার Node.js ভার্সন ১৮ বা তার চেয়ে বেশি।
